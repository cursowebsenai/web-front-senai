// Função da soma total da compra

function finalizarCompra() {
    total = subT1 + subT2 + subT3 + subT4 + subT5 + subT6 + subT7 + subT8 + subT9 + subT10 + subT11 + subT12 + subT13 + subT14 + subT15 + subT16 + subT17 + subT18 + subT19 + subT20 + subT21 + subT22 + subT23 + subT24 + subT25 + subT26 + subT27;
    document.getElementById('totalGeral').value = total.toFixed(2);
}


//Funções do subtotal
var totalP = 0.0;
var subT1 = 0.0;
function subProdt1(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT1 = valor * qtd;
    document.getElementById('subtotal1').value = subT1.toFixed(2);
}


var subT2 = 0.0;
function subProdt2(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT2 = valor * qtd;
    document.getElementById('subtotal2').value = subT2.toFixed(2);
}

var subT3 = 0.0;
function subProdt3(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT3 = valor * qtd;
    document.getElementById('subtotal3').value = subT3.toFixed(2);
}

var subT4 = 0.0;
function subProdt4(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT4 = valor * qtd;
    document.getElementById('subtotal4').value = subT4.toFixed(2);
}

var subT5 = 0.0;
function subProdt5(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT5 = valor * qtd;
    document.getElementById('subtotal5').value = subT5.toFixed(2);
}

var subT6 = 0.0;
function subProdt6(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT6 = valor * qtd;
    document.getElementById('subtotal6').value = subT6.toFixed(2);
}

var subT7 = 0.0;
function subProdt7(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT7 = valor * qtd;
    document.getElementById('subtotal7').value = subT7.toFixed(2);
}


var subT8 = 0.0;
function subProdt8(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT8 = valor * qtd;
    document.getElementById('subtotal8').value = subT8.toFixed(2);
}


var subT9 = 0.0;
function subProdt9(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT9 = valor * qtd;
    document.getElementById('subtotal9').value = subT9.toFixed(2);
}


var subT10 = 0.0;
function subProdt10(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT10 = valor * qtd;
    document.getElementById('subtotal10').value = subT10.toFixed(2);
}

var subT11 = 0.0;
function subProdt11(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT11 = valor * qtd;
    document.getElementById('subtotal11').value = subT11.toFixed(2);
}

var subT12 = 0.0;
function subProdt12(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT12 = valor * qtd;
    document.getElementById('subtotal12').value = subT12.toFixed(2);
}


var subT13 = 0.0;
function subProdt13(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT13 = valor * qtd;
    document.getElementById('subtotal13').value = subT13.toFixed(2);
}


var subT14 = 0.0;
function subProdt14(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT14 = valor * qtd;
    document.getElementById('subtotal14').value = subT14.toFixed(2);
}


var subT15 = 0.0;
function subProdt15(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT15 = valor * qtd;
    document.getElementById('subtotal15').value = subT15.toFixed(2);
}


var subT16 = 0.0;
function subProdt16(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT16 = valor * qtd;
    document.getElementById('subtotal16').value = subT16.toFixed(2);
}


var subT17 = 0.0;
function subProdt17(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT17 = valor * qtd;
    document.getElementById('subtotal17').value = subT17.toFixed(2);
}

var subT18 = 0.0;
function subProdt18(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT18 = valor * qtd;
    document.getElementById('subtotal18').value = subT18.toFixed(2);
}


var subT19 = 0.0;
function subProdt19(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT19 = valor * qtd;
    document.getElementById('subtotal19').value = subT19.toFixed(2);
}

var subT20 = 0.0;
function subProdt20(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT20 = valor * qtd;
    document.getElementById('subtotal3').value = subT20.toFixed(2);
}


var subT21 = 0.0;
function subProdt21(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT21 = valor * qtd;
    document.getElementById('subtotal21').value = subT21.toFixed(2);
}


var subT22 = 0.0;
function subProdt22(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT22 = valor * qtd;
    document.getElementById('subtotal22').value = subT22.toFixed(2);
}


var subT23 = 0.0;
function subProdt23(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT23 = valor * qtd;
    document.getElementById('subtotal23').value = subT23.toFixed(2);
}


var subT24 = 0.0;
function subProdt24(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT24 = valor * qtd;
    document.getElementById('subtotal24').value = subT24.toFixed(2);
}


var subT25 = 0.0;
function subProdt25(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT25 = valor * qtd;
    document.getElementById('subtotal25').value = subT25.toFixed(2);
}


var subT26 = 0.0;
function subProdt26(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT26 = valor * qtd;
    document.getElementById('subtotal26').value = subT26.toFixed(2);
}


var subT27 = 0.0;
function subProdt27(v, q) {
    var valor = parseFloat(v);
    var qtd = parseInt(q);
    subT27 = valor * qtd;
    document.getElementById('subtotal27').value = subT27.toFixed(2);
}

